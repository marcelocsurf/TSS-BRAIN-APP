// ═══ /gift/[code] — alias del link de regalo del libro ONE WAVE ═══
// Misma página que /one-wave/[code], servida DIRECTO (sin redirect): WhatsApp
// no sigue redirecciones al armar la vista previa y el link quedaba sin
// portada (Marcelo 2026-09-15). Los links /gift/… ya enviados siguen vivos.

export const dynamic = 'force-dynamic';
export const fetchCache = 'force-no-store';

export { default, metadata } from '../../one-wave/[code]/page';
